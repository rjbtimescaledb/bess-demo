FROM node:22-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY src ./src
COPY tsconfig.json ./
# Dense backfill: 10s intervals, ~1.73B rows, avoiding OSM ghost range
CMD ["npx", "tsx", "src/backfill.ts", "--days=1000", "--dense"]
