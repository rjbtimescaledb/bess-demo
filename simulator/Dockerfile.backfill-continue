FROM node:22-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY src ./src
COPY tsconfig.json ./
# Continue dense backfill from Oct 15, 2023 to now (~933 days remaining)
CMD ["npx", "tsx", "src/backfill.ts", "--days=933", "--dense"]
