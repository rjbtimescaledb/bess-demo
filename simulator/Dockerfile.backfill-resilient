FROM node:22-slim
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY src ./src
COPY tsconfig.json ./
# Resilient dense backfill: resumes from where it left off, 1000 days target
CMD ["npx", "tsx", "src/backfill.ts", "--days=1000", "--dense", "--resume"]
